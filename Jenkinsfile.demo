@Library('pipeline-library-demo')_


pipeline {
    agent any
    stages {
        stage('Demo') {
            steps {
                mavn 'https://github.com/vinay1074/web_app.git'
            }
        }
    }
}
